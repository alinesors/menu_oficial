[package]
name = "menu_ui_rust"
version = "0.1.0"
edition = "2021"

[lib]
crate-type = ["cdylib"]

[dependencies]
godot = { version = "0.2", features = ["experimental-threads"] }

[profile.dev]
opt-level = 1

[profile.release]
opt-level = 3
lto = "thin"
